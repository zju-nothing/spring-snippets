<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd ">


    <!-- This context will contain the beans from middle-tier services,dal layer etc. But not contain the web(or controller) layer beans -->
    <!-- The web(or controller) beans will be in the WebApplicationContext, which containing controllers,view resolvers, and other web-related beans -->
    <import resource="login-demo-dal.xml"/>
    <import resource="login-demo-service.xml"/>

</beans>